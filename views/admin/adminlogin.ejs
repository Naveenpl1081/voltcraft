<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Login - VoltCraft</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

    <style>
        .input-field {
            width: 100%;
            padding: 8px 0;
            border-bottom: 1px solid #E5E7EB;
            outline: none;
            font-size: 14px;
            color: #333;
        }

        .input-field::placeholder {
            color: #8B96A5;
        }

        .btn-primary {
            background-color: #007bff;
            color: white;
            width: 100%;
            padding: 10px;
            border-radius: 4px;
            font-size: 14px;
            font-weight: 500;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        .btn-primary:hover {
            background-color: #0056b3;
        }

        .footer-link {
            color: #666666;
            font-size: 13px;
            transition: color 0.3s;
        }

        .footer-link:hover {
            color: #007bff;
        }
    </style>
</head>
<body class="text-gray-800">

    <!-- Header -->
    <header class="py-4 px-8 border-b flex justify-between items-center bg-gray-900 text-white">
        <div class="text-lg font-bold">
            <span class="text-white">VoltCraft</span>
            <span class="text-gray-400">Admin Panel</span>
        </div>
    </header>

    <!-- Main Content -->
    <main class="flex flex-col items-center justify-center min-h-screen px-6 md:px-16 py-8">
        <!-- Login Form -->
        <div class="flex-1 max-w-md w-full bg-white p-8 shadow-md rounded-md">
            <h1 class="text-2xl font-semibold text-center mb-6">Admin Login</h1>
            <p class="text-sm text-gray-600 text-center mb-6">Enter your admin credentials to access the dashboard</p>
            
            <form action="/admin/login" method="post" class="space-y-4">
                <input type="text" placeholder="Admin Username" class="input-field" name="username" required>
                <input type="password" placeholder="Password" class="input-field" name="password" required>
                
                <% if (locals.message && locals.message.length > 0) { %>
                    <div class="alert alert-danger text-center">
                        <%= locals.message %>
                    </div>
                <% } %>

                <button type="submit" class="btn-primary">Log In</button>
            </form>

            <div class="mt-6 text-sm text-center">
                <a href="#" class="text-[#007bff]">Forgot Password?</a>
            </div>
        </div>
    </main>

    <!-- Footer -->
    <footer class="bg-gray-900 text-white py-4 text-center text-sm">
        <p>© 2024 VoltCraft Admin Panel. All rights reserved.</p>
    </footer>

</body>
</html>
